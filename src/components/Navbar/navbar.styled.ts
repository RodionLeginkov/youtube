import { Stack } from '@mui/system';
import styled from 'styled-components';

export const NavbarCointainer = styled(Stack)`
  position: sticky;
  background-color: #000;
  top: 0;
  display: flex;
  justify-content: space-between;
`;
